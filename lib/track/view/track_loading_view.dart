import 'package:flutter/material.dart';

class TrackLoading extends StatelessWidget {
  const TrackLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Little steps, help you go the distance'),
      ),
    );
  }
}
