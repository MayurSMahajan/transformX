import 'package:flutter/material.dart';

class StatsErrorView extends StatelessWidget {
  const StatsErrorView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Something went wrong, Try Again Later 😥'),
    );
  }
}
